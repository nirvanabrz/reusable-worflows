name: "PRD Deploy"

on:
  workflow_call:

jobs:
  # build:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Hello world
  #       run: echo Hello world!
        
  deploy:
    runs-on: ubuntu-latest
    environment: 
      name: prd
      url: 'https://mendespedrosa.com.br'
    steps:
      - name: Hello world
        run: echo Hello world! - PRD          
